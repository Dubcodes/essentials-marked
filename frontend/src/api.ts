export async function api(path:string,init?:RequestInit){const r=await fetch('/api'+path,{credentials:'include',headers:{'Content-Type':'application/json',...init?.headers},...init});if(!r.ok){const payload=await r.json().catch(()=>({detail:r.statusText}));const error:any=new Error(typeof payload.detail==='string'?payload.detail:JSON.stringify(payload.detail));error.status=r.status;throw error}return r.headers.get('content-type')?.includes('json')?r.json():r.text()}
