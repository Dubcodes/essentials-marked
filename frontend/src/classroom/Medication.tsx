import React,{useEffect,useState}from'react';import{api}from'../api';import{Choice,Workflow}from'./OrdinaryWorkflows';import{operationId,type WorkflowProps}from'./types';
type Medicine={id:string;child_name:string;medication_name:string;dose:string;route:string;scheduled_times:string[];instructions?:string;status:string;received:boolean;receipt_id?:string;returned_at?:string};
export const receiptChecksComplete=(checks:{label:boolean;authority:boolean;expiry:boolean})=>checks.label&&checks.authority&&checks.expiry;
export function Medication(props:WorkflowProps){const[items,setItems]=useState<Medicine[]>([]),[selected,setSelected]=useState<Medicine>(),[checks,setChecks]=useState({label:false,authority:false,expiry:false}),[form,setForm]=useState<any>({outcome:'given'}),[pin,setPin]=useState(''),[op,setOp]=useState(()=>operationId('medicine'));const load=()=>api('/classroom/medications').then(setItems);useEffect(()=>{void load()},[]);const receive=async()=>{try{await api('/medication/receipts',{method:'POST',body:JSON.stringify({authority_id:selected!.id,staff_id:props.staffId,handed_by:form.handed_by,label_checked:checks.label,authority_matched:checks.authority,expiry_checked:checks.expiry,storage_location:form.storage_location,quantity:form.quantity,note:form.note})});props.notice('Medication receipt confirmed');await load();setSelected(undefined);await props.refresh()}catch(e:any){props.notice(e.message)}};const administer=async()=>{try{await api('/classroom/medication/administrations',{method:'POST',body:JSON.stringify({client_operation_id:op,authority_id:selected!.id,room_id:props.roomId,staff_id:props.staffId,staff_pin:pin,outcome:form.outcome,dose:selected!.dose,note:form.note})});setOp(operationId('medicine'));setPin('');props.notice('Medication administration recorded');await load();setSelected(undefined);await props.refresh()}catch(e:any){setPin('');props.notice(e.message)}};const returned=async()=>{try{await api(`/medication/receipts/${selected!.receipt_id}/return`,{method:'POST',body:JSON.stringify({staff_id:props.staffId,returned_to:form.returned_to})});props.notice('Medication marked returned');await load();setSelected(undefined)}catch(e:any){props.notice(e.message)}};if(selected)return <Workflow title={selected.received?'Administer medication':'Receive medication'} close={()=>setSelected(undefined)}><section className="authority"><h3>{selected.child_name} · {selected.medication_name}</h3><p><b>Dose:</b> {selected.dose} · <b>Route:</b> {selected.route}</p><p><b>Scheduled:</b> {selected.scheduled_times?.join(', ')||'As directed'}</p><p>{selected.instructions}</p></section>{!selected.received?<><Check label="Correct label" checked={checks.label} set={label=>setChecks({...checks,label})}/><Check label="Authority matches" checked={checks.authority} set={authority=>setChecks({...checks,authority})}/><Check label="Expiry checked" checked={checks.expiry} set={expiry=>setChecks({...checks,expiry})}/>{['handed_by','storage_location','quantity'].map(key=><label key={key}>{key.replaceAll('_',' ')}<input value={form[key]||''} onChange={e=>setForm({...form,[key]:e.target.value})}/></label>)}<label>Optional note<input value={form.note||''} onChange={e=>setForm({...form,note:e.target.value})}/></label><button className="save" disabled={!receiptChecksComplete(checks)} onClick={receive}>Confirm physical receipt</button></>:<><Choice label="Outcome" values={['given','refused','partly_taken','spat_out','vomited_afterward','missed','parent_administered','other']} value={form.outcome} set={outcome=>setForm({...form,outcome})}/><label>Optional note<input value={form.note||''} onChange={e=>setForm({...form,note:e.target.value})}/></label><label>Staff PIN — required only now<input aria-label="Medication staff PIN" type="password" value={pin} onChange={e=>setPin(e.target.value)} autoComplete="off"/></label><button className="save" disabled={!pin} onClick={administer}>Verify PIN and record once</button><details><summary>Return medicine</summary><label>Returned to<input value={form.returned_to||''} onChange={e=>setForm({...form,returned_to:e.target.value})}/></label><button className="minor" disabled={!form.returned_to} onClick={returned}>Mark returned</button></details></>}</Workflow>;return <Workflow title="Medicine" close={props.close}><h3>Awaiting receipt</h3>{items.filter(x=>x.status==='authorised'&&!x.received&&!x.returned_at).map(x=><MedicineButton key={x.id} item={x} pick={setSelected}/>) }<h3>Active / due</h3>{items.filter(x=>x.received).map(x=><MedicineButton key={x.id} item={x} pick={setSelected}/>) }<h3>Completed / returned</h3>{items.filter(x=>x.returned_at).map(x=><p key={x.id}>{x.child_name} · {x.medication_name} · returned</p>)}{!items.length&&<p>No medication authorities for this centre.</p>}</Workflow>}
function Check({label,checked,set}:{label:string;checked:boolean;set:(value:boolean)=>void}){return <label className="check"><input type="checkbox" checked={checked} onChange={e=>set(e.target.checked)}/>{label}</label>}
function MedicineButton({item,pick}:{item:Medicine;pick:(item:Medicine)=>void}){return <button className="record-row" onClick={()=>pick(item)}><b>{item.child_name}</b><span>{item.medication_name} · {item.dose} · {item.route}</span></button>}
