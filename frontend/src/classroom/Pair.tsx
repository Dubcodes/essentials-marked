import React,{useState}from'react';import{api}from'../api';
export default function PairTablet(){const[token,setToken]=useState(()=>new URLSearchParams(location.search).get('token')||''),[challenge,setChallenge]=useState(''),[error,setError]=useState('');return <main className="login"><h1>Pair classroom tablet</h1><p>Confirm the device name and room shown by the administrator, then enter the three-digit challenge.</p><label>One-time token<input value={token} onChange={e=>setToken(e.target.value)}/></label><label>3-digit challenge<input inputMode="numeric" maxLength={3} value={challenge} onChange={e=>setChallenge(e.target.value.replace(/\D/g,''))}/></label><button disabled={!token||challenge.length!==3} onClick={()=>api('/device/pair',{method:'POST',body:JSON.stringify({token,challenge})}).then(()=>location.assign('/classroom')).catch(e=>setError(e.message))}>Pair securely</button>{error&&<p className="error">{error}</p>}</main>}
