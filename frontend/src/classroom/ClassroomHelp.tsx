import React from'react';
export function ClassroomHelp(){const topics=[['Pairing','Pair this tablet from Admin Devices using the QR token and separate challenge.'],['Attendance and visits','Arrive/depart children and start/end visits in Attendance / Presence.'],['Toileting','Select children, choose nappy or toilet and save the outcome.'],['Food','Record one atomic batch; a temporary outage stores the exact batch for sync.'],['Sunscreen','Select physically present children and record the application.'],['Sleep lifecycle','Use Put down, Fell asleep, Wake and Got up in order.'],['Sleep Check','Select sleeping children and confirm warmth, breathing and wellbeing.'],['Medication','Active means authorised and on site, not automatically due. Final administration requires a live PIN check.'],['Incident drafts','Partial drafts autosave after a child is selected. Review/PIN finalises; Discard removes local and server copies.'],['Parent notes','Open unread notes, mark read and pin when operationally useful.'],['Offline and sync','Ordinary care can queue. Open Sync for status; high-consequence finalisation requires connectivity.'],['Emergency Roll','Uses the last-confirmed attendance-only snapshot and groups children by physical room.'],['Troubleshooting','Keep an interrupted form open and retry. Re-pair expired devices. Never share PINs.']];return <section className="help-page">{topics.map(([title,body])=><article key={title}><h3>{title}</h3><p>{body}</p></article>)}</section>}
